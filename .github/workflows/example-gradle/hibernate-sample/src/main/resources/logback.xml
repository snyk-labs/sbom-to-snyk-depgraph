<configuration>
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <!--<pattern>%d{yyyy-MM-dd HH:mm:ss.SSS}|${HOSTNAME}|%contextName|%-5level|%msg ||%class:%line %n</pattern>-->
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS}|%contextName|%-5level|%msg ||%class:%line %n</pattern>
        </encoder>
    </appender>
    <!-- the console defaults to INFO level-->
    <root level="ERROR">
        <appender-ref ref="CONSOLE"/>
    </root>

    <logger name="org.hibernate.stat">
        <level value="DEBUG" />
    </logger>
</configuration>
